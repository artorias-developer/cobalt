FROM debian:bookworm-slim

ARG SERVER_ROOT

RUN apt-get update && \
    apt-get install -y \
        bash \
        libicu-dev \
        mono-runtime \
        wget && \
    rm -rf /var/lib/apt/lists/*

WORKDIR ${SERVER_ROOT}

ENTRYPOINT ["/bin/bash", "-c", "exec ./entrypoint.sh"]